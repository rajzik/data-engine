/**
 * Retype anything to string
 *
 * @param {any} item which should be retyped
 * @return {string} retyped string
 * @memberOf FilterValue
 */
const stringRetype = item => `${item}`;

export default stringRetype;
